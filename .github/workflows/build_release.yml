on:
  release:
    types: [ published ]
env:
  ORG_GRADLE_PROJECT_modVersion: "1.1.0.0"
jobs:
  PublishRelease:
    runs-on: ubuntu-latest
    steps:
    - name: Echo Version
      run: echo ${GITHUB_REF/refs\/tags\//v}
#    - uses: actions/checkout@v3
#    - uses: actions/setup-java@v3
#      with:
#        distribution: temurin
#        java-version: 17
#    - name: Setup Gradle
#      uses: gradle/gradle-build-action@v2
#    - name: Execute Gradle Build
#      run: ./gradlew build
#    - name: Build Jar
#      run: ./gradlew jar
#    - name: Build Reobf Jar
#      run: ./gradlew reobfJar